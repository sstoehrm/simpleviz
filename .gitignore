# build output
node_modules/
public/js/
dist/

# editor / LSP tooling: clj-kondo holds only its cache and imported library
# configs; an .lsp/config.edn stays committable
.clj-kondo/
.lsp/.cache/
.nrepl-port

# Claude Code session scratch
.claude/worktrees/
.superpowers/

# Local feature worktrees
.worktrees/
